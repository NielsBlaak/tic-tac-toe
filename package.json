{
  "name": "tic-tac-toe",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@capacitor-community/admob": "^3.2.0",
    "@capacitor/android": "^3.3.1",
    "@capacitor/app": "^1.1.1",
    "@capacitor/cli": "^3.5.1",
    "@capacitor/core": "^3.3.1",
    "@capacitor/ios": "^3.3.1",
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "cordova-res": "^0.15.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.3.0",
    "react-scripts": "4.0.3",
    "typescript": "^4.4.4",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build-android": "yarn build && npx cap copy && npx cap copy android && npx cap open android",
    "build-ios": "yarn build && npx cap copy && npx cap copy ios && npx cap open ios",
    "resources": "capacitor-resources -p android,ios"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
